# Open-source showcase mode: no backend, database, or API keys are required.
CONTENT_SOURCE=local

# Optional live-data mode. Set CONTENT_SOURCE=http and provide the upstream URL,
# then restart the frontend service.
CONTENT_API_BASE_URL=
CONTENT_API_TIMEOUT_MS=8000
